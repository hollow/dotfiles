Include %d/.config/ssh/config.crypto
Include %d/.config/colima/ssh_config

HashKnownHosts no
StrictHostKeyChecking no

# Prevent connections from beind dropped
ServerAliveInterval 900
ServerAliveCountMax 0
