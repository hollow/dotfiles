#!/usr/bin/env zsh

set -e

gh repo list \
    --no-archived \
    --limit 1000 \
    --json nameWithOwner "$@" |
jq -r '.[].nameWithOwner' |
parallel \
    --bar \
    --jobs 5 \
    --tagstring "[{}]" \
    git-clone-clean-main https://github.com/{} "${HOME}/src/{}"
