#!/usr/bin/env zsh

set -e

gh repo list \
    --archived \
    --limit 1000 \
    --json nameWithOwner "$@" |
jq -r '.[].nameWithOwner' |
parallel \
    --bar \
    --tagstring "[{}]" \
    rm -rf "${HOME}/src/{}"
